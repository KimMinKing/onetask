export interface SqldQuiz { question: string; options: string[]; answer: number; explanation: string }
export interface SqldLesson { id: number; section: string; title: string; subtitle: string; summary: string; points: string[]; sql?: string; trap: string; quiz: SqldQuiz }

function lesson(id: number, section: string, title: string, subtitle: string, core: string, trap: string, sql = ""): SqldLesson {
  const points = core.split(" | ");
  return {
    id, section, title, subtitle,
    summary: `${subtitle}\n\n이 단계에서는 정의를 외우는 데서 끝내지 않고, 보기의 조건과 SQL 실행 결과를 연결해 판단한다. 핵심 문장을 자기 말로 설명하고 예시의 결과를 손으로 먼저 계산해 보자.`,
    points, sql, trap,
    quiz: { question: `다음 중 ‘${title}’에 대한 설명으로 옳은 것은?`, options: [points[0], trap], answer: 0, explanation: points.join(" ") },
  };
}

export const SQLD_CURRICULUM: SqldLesson[] = [
  lesson(1,"데이터 모델링","모델링의 목적","현실 업무를 데이터 구조로 추상화한다.","데이터 모델은 업무 규칙과 데이터 관계를 명확히 표현한다. | 좋은 모델은 중복을 줄이고 일관성·유연성·소통 가능성을 높인다. | 모델링은 시스템 구현 전에 업무를 정확히 이해하는 과정이다.","모델링은 단순히 테이블을 그리는 작업이며 업무 규칙과 무관하다."),
  lesson(2,"데이터 모델링","모델링의 3대 특징","추상화·단순화·명확화를 구분한다.","추상화는 공통 특성을 뽑아 일정한 형식으로 표현하는 것이다. | 단순화는 복잡한 현실을 약속된 표기법으로 쉽게 이해하게 한다. | 명확화는 애매함 없이 정확하게 현상을 기술하는 것이다.","추상화는 상세 정보를 하나도 생략하지 않고 현실을 그대로 복제하는 것이다."),
  lesson(3,"데이터 모델링","관점별 데이터 모델","데이터·프로세스·상관 관점을 구별한다.","데이터 관점은 업무가 어떤 데이터와 구조로 이루어졌는지 본다. | 프로세스 관점은 업무가 실제로 무엇을 하는지 본다. | 상관 관점은 업무 처리와 데이터 변화의 관계를 본다.","프로세스 관점은 데이터 사이의 정적 관계만을 표현한다."),
  lesson(4,"데이터 모델링","모델링 3단계","개념·논리·물리 모델링의 산출물을 구별한다.","개념 모델은 핵심 엔터티와 관계를 조직 전체 관점에서 표현한다. | 논리 모델은 속성·식별자·정규화까지 DBMS와 독립적으로 구체화한다. | 물리 모델은 특정 DBMS의 테이블·컬럼·인덱스·타입으로 구현한다.","논리 모델링 단계에서 특정 DBMS의 저장공간과 인덱스를 확정해야 한다."),
  lesson(5,"데이터 모델링","스키마 3계층","외부·개념·내부 스키마와 독립성을 이해한다.","외부 스키마는 사용자별 관점, 개념 스키마는 전체 통합 관점이다. | 내부 스키마는 물리 저장 구조를 표현한다. | 논리적 독립성은 개념 스키마 변경이 외부 스키마에 미치는 영향을 최소화한다.","외부 스키마는 데이터베이스의 물리적 저장 방식을 정의한다."),
  lesson(6,"데이터 모델링","ERD 작성","엔터티 배치부터 관계명·차수·선택성까지 읽는다.","ERD는 엔터티를 배치하고 관계를 설정한 뒤 관계명·차수·선택성을 표기한다. | 관계명은 현재형 능동형으로 명확하게 작성한다. | ERD 배치 순서 자체가 데이터 모델의 품질을 결정하지는 않는다.","ERD에서는 관계명보다 선을 예쁘게 배치하는 것이 업무 의미보다 중요하다."),
  lesson(7,"데이터 모델링","엔터티의 조건","업무에 필요한 독립적 집합인지 판별한다.","엔터티는 업무에서 관리할 필요가 있는 명사형 대상이다. | 두 개 이상의 인스턴스와 두 개 이상의 속성을 갖는 것이 일반적이다. | 다른 엔터티와 최소 한 개 이상의 관계가 있어야 하나 통계성·코드성 엔터티는 예외가 가능하다.","인스턴스가 하나뿐인 대상도 언제나 올바른 엔터티다."),
  lesson(8,"데이터 모델링","엔터티 분류","유형·개념·사건 및 기본·중심·행위를 구분한다.","유형 엔터티는 물리적 형태가 있고, 개념 엔터티는 개념적 정보다. | 사건 엔터티는 업무 수행에 따라 발생하며 데이터가 많이 증가한다. | 발생 시점 기준으로 기본/키, 중심, 행위 엔터티로도 나눈다.","주문내역처럼 거래 때마다 늘어나는 데이터는 기본 엔터티에 해당한다."),
  lesson(9,"데이터 모델링","엔터티 명명 규칙","현업 용어로 단수 명사를 사용한다.","업무에서 사용하는 용어를 사용하고 약어를 남용하지 않는다. | 단수 명사를 쓰며 유일한 이름을 부여한다. | 생성 의미를 분명히 하고 다른 엔터티와 의미가 겹치지 않게 한다.","엔터티명은 구현 편의를 위해 의미 없는 영문 약어만 사용하는 것이 좋다."),
  lesson(10,"데이터 모델링","속성의 정의","인스턴스를 설명하는 최소 데이터 단위다.","속성은 업무에서 관리해야 하는 의미상 더 분리되지 않는 최소 데이터 단위다. | 하나의 속성은 같은 종류의 값만 가지며 하나의 인스턴스에 한 값이 원칙이다. | 속성은 엔터티에 대한 구체적 정보를 제공한다.","하나의 속성에 전화번호와 주소를 함께 저장해도 원자성이 유지된다."),
  lesson(11,"데이터 모델링","속성 분류","기본·설계·파생 속성의 차이를 익힌다.","기본 속성은 업무로부터 원래 존재하는 정보다. | 설계 속성은 모델링 과정에서 업무 규칙을 위해 만든다. | 파생 속성은 다른 속성으로 계산되며 정합성 관리가 필요하다.","합계처럼 계산 가능한 파생 속성은 언제나 저장해야 정규화가 좋아진다."),
  lesson(12,"데이터 모델링","도메인과 NULL","속성의 값 범위와 미정 상태를 구별한다.","도메인은 속성이 가질 수 있는 타입·크기·제약·값 범위다. | NULL은 0이나 공백이 아니라 값이 없거나 알려지지 않은 상태다. | 같은 도메인의 속성끼리 비교·연산하는 것이 의미상 자연스럽다.","NULL과 숫자 0은 동일한 값이므로 비교 결과도 항상 같다."),
  lesson(13,"데이터 모델링","관계의 정의","연관성과 의존성을 관계로 표현한다.","관계는 엔터티 인스턴스 사이의 논리적 연관성이다. | 존재 관계는 소속처럼 상태를, 행위 관계는 주문처럼 행위를 통해 생긴 연결을 나타낸다. | 관계 패어링은 두 인스턴스 사이의 개별 연결이다.","관계는 반드시 물리적 외래키가 있어야만 개념 모델에 표현할 수 있다."),
  lesson(14,"데이터 모델링","관계 표기법","관계명·차수·선택성을 읽는다.","차수는 1:1, 1:M, M:N처럼 참여 인스턴스 수를 뜻한다. | 선택성은 필수 참여인지 선택 참여인지 나타낸다. | 관계는 양방향 관점에서 관계명을 읽어 업무 규칙과 일치하는지 검증한다.","1:M 관계에서 M쪽 인스턴스는 항상 정확히 하나만 존재한다."),
  lesson(15,"데이터 모델링","식별자의 종류","주·보조, 내부·외부, 단일·복합을 구분한다.","주식별자는 대표성을 가지며 엔터티 인스턴스를 유일하게 구분한다. | 외부식별자는 관계를 통해 부모 엔터티에서 받아온 식별자다. | 복합식별자는 두 개 이상의 속성 조합으로 유일성을 만든다.","외부식별자는 반드시 해당 엔터티 내부에서 새로 만들어진 속성이다."),
  lesson(16,"데이터 모델링","주식별자 특징","유일성·최소성·불변성·존재성을 암기한다.","유일성은 각 인스턴스를 고유하게 식별함을 뜻한다. | 최소성은 유일성을 만족하는 최소 속성 조합이어야 함을 뜻한다. | 불변성과 존재성 때문에 주식별자 값은 자주 변하거나 NULL이어서는 안 된다.","주식별자는 업무 변화에 따라 자주 수정되는 속성일수록 적합하다."),
  lesson(17,"데이터 모델링","본질·인조 식별자","업무 식별자와 대리키의 장단점을 비교한다.","본질식별자는 업무에서 자연스럽게 발생한 속성으로 구성된다. | 인조식별자는 편의와 성능을 위해 만든 대리키다. | 인조키를 써도 업무상 유일해야 하는 본질 속성에는 별도 UNIQUE 제약이 필요할 수 있다.","인조식별자를 기본키로 만들면 업무 중복 데이터가 자동으로 방지된다."),
  lesson(18,"데이터 모델링","식별·비식별 관계","부모 식별자의 자식 PK 포함 여부를 판단한다.","식별 관계는 부모 식별자가 자식 주식별자의 일부가 된다. | 비식별 관계는 부모 식별자가 자식의 일반 외래키가 된다. | 식별 관계가 과도하면 복합키가 길어지고 변경 영향이 커질 수 있다.","비식별 관계에서는 부모 키가 자식 테이블에 어떤 형태로도 전달되지 않는다."),
  lesson(19,"데이터 모델링","제1정규형","반복 속성과 다중값을 제거한다.","1NF는 모든 속성값이 원자값이어야 한다. | 반복 그룹을 별도 행 또는 별도 엔터티로 분리한다. | 한 컬럼에 쉼표로 여러 상품코드를 저장하면 관계형 연산과 무결성이 어려워진다.","한 컬럼에 여러 값을 구분자로 저장해도 문자열 하나이므로 1NF를 만족한다."),
  lesson(20,"데이터 모델링","제2정규형","부분 함수 종속을 제거한다.","2NF는 1NF를 만족하고 기본키가 아닌 속성이 전체 기본키에 완전 함수 종속이어야 한다. | 복합키 일부에만 종속되는 속성을 분리한다. | 단일 속성 기본키인 테이블에는 부분 함수 종속이 발생하지 않는다.","2NF는 이행 함수 종속을 제거하는 정규형이다."),
  lesson(21,"데이터 모델링","제3정규형과 BCNF","이행 종속과 결정자를 정리한다.","3NF는 일반 속성 사이의 이행 함수 종속을 제거한다. | BCNF는 모든 결정자가 후보키가 되도록 하는 더 강한 정규형이다. | 정규화는 삽입·수정·삭제 이상 현상을 줄인다.","3NF는 복합키의 일부에 종속되는 부분 함수 종속만 제거한다."),
  lesson(22,"데이터 모델링","정규화와 SQL 성능","정규화의 조인 증가와 입력 성능 효과를 함께 본다.","정규화로 데이터 중복이 줄어 입력·수정·삭제 성능과 정합성이 좋아질 수 있다. | 조회 시 조인이 늘어날 수 있지만 항상 성능이 나빠지는 것은 아니다. | 함수 종속성에 근거해 정규화 여부를 판단해야 한다.","조회 조인이 생긴다는 이유만으로 정규화는 언제나 성능을 악화시킨다."),
  lesson(23,"데이터 모델링","관계와 조인","모델의 관계가 SQL 조인 조건으로 구현된다.","관계가 정의된 컬럼은 조인의 연결 조건이 된다. | 관계가 누락되면 필요한 조인 조건을 발견하기 어렵고 카티션 곱 위험이 커진다. | M:N 관계는 보통 연결 엔터티로 해소한다.","M:N 관계는 관계형 테이블 하나의 외래키만으로 그대로 구현하는 것이 원칙이다."),
  lesson(24,"데이터 모델링","NULL 속성 모델링","필수·선택 관계와 집계 결과를 연결한다.","선택 관계의 외래키는 NULL을 허용할 수 있다. | NULL 포함 컬럼의 산술 연산 결과는 일반적으로 NULL이다. | COUNT(*)는 행을 세고 COUNT(컬럼)은 NULL을 제외한다.","COUNT(컬럼)은 NULL인 행까지 모두 세므로 COUNT(*)와 항상 같다.","SELECT COUNT(*), COUNT(manager_id) FROM employee;"),
  lesson(25,"데이터 모델링","트랜잭션과 모델","업무 단위가 관계 필수성과 테이블 경계를 결정한다.","하나의 트랜잭션에서 함께 생성·변경되는 데이터 관계를 모델에 반영한다. | 필수 관계 누락은 데이터 고립과 조인 누락을 만들 수 있다. | 모델은 정적 구조뿐 아니라 업무 트랜잭션의 데이터 변화를 표현해야 한다.","데이터 모델은 정적 구조만 다루므로 트랜잭션 발생 순서는 검토할 필요가 없다."),
  lesson(26,"SQL 기본","관계형 데이터베이스","릴레이션·튜플·속성과 키를 SQL 용어로 연결한다.","테이블은 릴레이션, 행은 튜플, 열은 속성에 대응한다. | 기본키는 유일성과 NOT NULL을 보장하고 외래키는 참조 무결성을 지원한다. | SQL은 집합을 대상으로 하는 선언적 언어다.","SQL은 처리 절차를 한 줄씩 반드시 지정하는 절차적 언어다."),
  lesson(27,"SQL 기본","SELECT 논리 실행 순서","FROM부터 ORDER BY까지 순서를 암기한다.","대표 논리 순서는 FROM→WHERE→GROUP BY→HAVING→SELECT→ORDER BY다. | SELECT 별칭은 같은 단계보다 뒤인 ORDER BY에서 사용할 수 있다. | WHERE는 그룹화 전에 행을 제거한다.","SELECT 절이 가장 먼저 실행되므로 SELECT 별칭을 WHERE에서 항상 사용할 수 있다.","SELECT deptno, COUNT(*) cnt FROM emp WHERE sal >= 1000 GROUP BY deptno HAVING COUNT(*) >= 2 ORDER BY cnt DESC;"),
  lesson(28,"SQL 기본","SELECT와 DISTINCT","투영 컬럼 조합의 중복을 제거한다.","DISTINCT는 SELECT에 나열된 전체 컬럼 조합을 기준으로 중복을 제거한다. | ALL은 기본값이며 중복을 유지한다. | DISTINCT를 함수처럼 한 컬럼에만 적용한다고 오해하지 않는다.","SELECT DISTINCT a, b는 a 컬럼만 중복 제거하고 b는 무관하다.","SELECT DISTINCT deptno, job FROM emp;"),
  lesson(29,"SQL 기본","산술 연산과 NULL","NULL 전파 규칙을 계산한다.","숫자 산술식에 NULL이 포함되면 결과는 일반적으로 NULL이다. | NULL을 대체하려면 COALESCE 또는 DBMS별 NVL/IFNULL을 사용한다. | 문자열 결합과 NULL 처리는 DBMS별 차이를 확인한다.","급여 + NULL은 급여와 동일하다.","SELECT salary + COALESCE(commission, 0) AS pay FROM employee;"),
  lesson(30,"SQL 기본","별칭과 문자열","식별자 별칭과 문자열 리터럴을 구분한다.","문자열 리터럴은 작은따옴표를 사용한다. | 별칭에 공백·특수문자·대소문자 보존이 필요하면 DBMS의 식별자 인용 규칙을 따른다. | 별칭은 결과 컬럼의 표시 이름을 바꿀 뿐 원본 컬럼명을 변경하지 않는다.","SELECT 별칭을 지정하면 테이블의 실제 컬럼명이 영구 변경된다."),
  lesson(31,"SQL 기본","비교 연산자","=, <>, BETWEEN, IN의 경계를 읽는다.","BETWEEN a AND b는 a와 b 경계를 모두 포함한다. | IN은 여러 OR 동등 비교를 간결하게 표현한다. | NOT은 NULL과 결합될 때 UNKNOWN을 만들 수 있다.","BETWEEN 10 AND 20은 10과 20을 제외한다.","SELECT * FROM emp WHERE salary BETWEEN 3000 AND 5000;"),
  lesson(32,"SQL 기본","LIKE와 와일드카드","%, _ 및 ESCAPE의 의미를 구분한다.","%는 길이 0 이상의 문자열, _는 정확히 한 문자를 뜻한다. | 리터럴 %, _를 찾으려면 ESCAPE 문자를 정할 수 있다. | 앞에 %가 붙는 검색은 일반 B-tree 인덱스 활용이 어려운 경우가 많다.","LIKE 'A_'는 A로 시작하면 뒤 길이와 관계없이 모두 일치한다.","SELECT * FROM member WHERE name LIKE '김_';"),
  lesson(33,"SQL 기본","NULL 비교","IS NULL과 3값 논리를 이해한다.","NULL은 = NULL로 비교하지 않고 IS NULL을 사용한다. | NULL과의 비교 결과는 TRUE/FALSE가 아닌 UNKNOWN이다. | WHERE는 TRUE인 행만 남기므로 UNKNOWN 행은 제외된다.","col = NULL은 col이 NULL인 행을 정확히 찾는다.","SELECT * FROM emp WHERE commission IS NULL;"),
  lesson(34,"SQL 기본","AND·OR·NOT 우선순위","괄호로 조건 의도를 고정한다.","일반적으로 NOT, AND, OR 순으로 평가된다. | 혼합 조건은 괄호를 사용해 업무 의도를 분명히 한다. | 드모르간 법칙으로 NOT 조건의 동치를 확인할 수 있다.","OR가 AND보다 먼저 계산되므로 괄호가 없어도 같은 결과다."),
  lesson(35,"SQL 기본","문자 함수","대소문자·길이·부분문자열 함수를 익힌다.","UPPER/LOWER는 대소문자를 변환하고 LENGTH 계열은 길이를 구한다. | SUBSTR/SUBSTRING은 일부 문자열을 추출한다. | TRIM은 기본적으로 양끝의 지정 문자 또는 공백을 제거하며 내부 공백은 제거하지 않는다.","TRIM은 문자열 중간의 모든 공백까지 제거한다.","SELECT UPPER(name), SUBSTR(name, 1, 2), LENGTH(name) FROM member;"),
  lesson(36,"SQL 기본","숫자 함수","ROUND·TRUNC·MOD 결과를 계산한다.","ROUND는 지정 자리에서 반올림하고 TRUNC는 버림 처리한다. | MOD는 나머지를 반환한다. | 음수 자리수는 일·십·백 단위 쪽 처리를 뜻할 수 있다.","ROUND와 TRUNC는 모든 입력에서 같은 값을 반환한다.","SELECT ROUND(123.456, 1), TRUNC(123.456, 1), MOD(10, 3);"),
  lesson(37,"SQL 기본","날짜 연산","날짜±숫자와 날짜 차이를 DBMS 기준으로 읽는다.","Oracle에서 날짜에 숫자 1을 더하면 하루가 증가한다. | 날짜 차이는 일수로 계산될 수 있고 DBMS마다 타입·함수가 다르다. | 월말·윤년 처리는 ADD_MONTHS 같은 날짜 함수를 활용한다.","모든 DBMS에서 날짜 + 1은 정확히 한 시간을 더한다."),
  lesson(38,"SQL 기본","명시적 형변환","TO_CHAR·TO_DATE·CAST로 타입을 통제한다.","명시적 변환은 변환 형식과 의도를 코드에 드러낸다. | 날짜 문자열은 포맷 모델과 값이 일치해야 한다. | 암시적 변환은 세션 설정과 DBMS에 따라 오류·성능 저하를 만들 수 있다.","암시적 형변환은 환경과 무관하게 언제나 동일하고 안전하다.","SELECT TO_CHAR(hiredate, 'YYYY-MM-DD') FROM emp;"),
  lesson(39,"SQL 기본","CASE 표현식","조건별 값을 반환하는 SQL 표현식이다.","단순 CASE는 한 표현식과 여러 값을 비교한다. | 검색 CASE는 각 WHEN에 독립 조건을 쓴다. | 일치하는 WHEN이 없고 ELSE도 없으면 NULL을 반환한다.","CASE에 ELSE가 없으면 조건 불일치 시 항상 오류가 발생한다.","SELECT CASE WHEN salary >= 5000 THEN 'HIGH' ELSE 'NORMAL' END grade FROM emp;"),
  lesson(40,"SQL 기본","NULL 처리 함수","COALESCE·NULLIF·NVL의 차이를 익힌다.","COALESCE는 인자 중 처음 NULL이 아닌 값을 반환한다. | NULLIF(a,b)는 두 값이 같으면 NULL, 다르면 a를 반환한다. | NVL은 Oracle 전용이며 데이터 타입 변환 규칙을 주의한다.","NULLIF(a,b)는 두 값이 다르면 언제나 b를 반환한다."),
  lesson(41,"SQL 기본","집계 함수","행 집합을 하나의 요약값으로 만든다.","COUNT(*)를 제외한 다수 집계 함수는 NULL을 제외한다. | COUNT(DISTINCT col)은 NULL을 제외한 서로 다른 값 수를 센다. | NULL만 있는 집합에서 SUM/AVG는 0이 아니라 NULL일 수 있다.","AVG(col)은 NULL을 0으로 바꾸어 분모에 포함한다.","SELECT COUNT(*), COUNT(comm), AVG(salary) FROM emp;"),
  lesson(42,"SQL 기본","GROUP BY","그룹 기준과 SELECT 표현의 규칙을 지킨다.","집계되지 않은 SELECT 컬럼은 원칙적으로 GROUP BY에 포함해야 한다. | WHERE는 그룹화 전 행을 필터링한다. | 그룹 기준이 여러 개면 컬럼 조합별로 그룹이 만들어진다.","GROUP BY가 있으면 SELECT에 어떤 일반 컬럼이든 자유롭게 표시할 수 있다."),
  lesson(43,"SQL 기본","HAVING","집계 결과로 그룹을 필터링한다.","HAVING은 GROUP BY 이후 그룹에 조건을 적용한다. | 집계 조건은 HAVING에 작성한다. | 그룹화 전 제거 가능한 일반 행 조건은 WHERE에 두는 편이 명확하고 효율적이다.","WHERE COUNT(*) > 1처럼 WHERE에서 집계 결과를 직접 필터링하는 것이 표준이다.","SELECT deptno, COUNT(*) FROM emp GROUP BY deptno HAVING COUNT(*) >= 3;"),
  lesson(44,"SQL 기본","ORDER BY","정렬 방향·NULL·표현식 정렬을 이해한다.","ASC가 기본 정렬 방향이고 DESC는 내림차순이다. | 여러 정렬 키는 앞 키가 같은 행에 다음 키가 적용된다. | 정렬을 명시하지 않은 조회 결과의 행 순서는 보장되지 않는다.","기본키가 있는 테이블은 ORDER BY 없이도 항상 기본키 순서로 반환된다."),
  lesson(45,"SQL 기본","카티션 곱","조인 조건 누락 시 행 수를 계산한다.","조인 조건이 없으면 두 집합의 모든 조합이 생성된다. | A가 m행, B가 n행이면 카티션 곱은 m×n행이다. | CROSS JOIN은 의도적 카티션 곱을 명시한다.","두 테이블의 조인 조건이 없으면 행 수는 m+n이다.","SELECT * FROM A CROSS JOIN B;"),
  lesson(46,"SQL 기본","등가 조인","PK와 FK의 동등 조건으로 행을 연결한다.","INNER JOIN은 조인 조건을 만족하는 행만 반환한다. | 같은 컬럼명이 있으면 테이블 별칭으로 모호성을 제거한다. | 조인 조건과 일반 필터 조건을 구분해 읽는다.","INNER JOIN은 조인에 실패한 양쪽 행도 NULL로 채워 반환한다.","SELECT e.name, d.dname FROM emp e JOIN dept d ON e.deptno = d.deptno;"),
  lesson(47,"SQL 기본","비등가 조인","범위 조건으로 등급·구간을 연결한다.","비등가 조인은 <, >, BETWEEN 같은 조건으로 행을 연결한다. | 급여 구간과 등급표 매핑이 대표 사례다. | 구간이 겹치면 한 행이 여러 행과 조인될 수 있다.","조인 조건에는 등호만 사용할 수 있다."),
  lesson(48,"SQL 기본","LEFT OUTER JOIN","보존되는 테이블을 판별한다.","LEFT JOIN은 왼쪽 테이블의 모든 행을 보존한다. | 매칭되지 않은 오른쪽 컬럼은 NULL이 된다. | 오른쪽 컬럼 조건을 WHERE에 두면 NULL 행이 제거되어 INNER JOIN처럼 변할 수 있다.","LEFT JOIN 뒤 WHERE 오른쪽컬럼 = 값 조건을 써도 매칭 실패 행은 항상 보존된다.","SELECT d.dname, e.name FROM dept d LEFT JOIN emp e ON d.deptno=e.deptno;"),
  lesson(49,"SQL 기본","FULL OUTER JOIN","양쪽 미매칭 행까지 보존한다.","FULL OUTER JOIN은 양쪽의 매칭 행과 양쪽 각각의 미매칭 행을 모두 반환한다. | 미매칭 반대편 컬럼은 NULL이다. | DBMS가 직접 지원하지 않으면 다른 집합 연산으로 구현할 수 있다.","FULL OUTER JOIN은 왼쪽 테이블의 행만 보존한다."),
  lesson(50,"SQL 기본","표준 JOIN 문법","ON·USING·NATURAL JOIN을 구분한다.","ON은 임의의 조인 조건을 명시할 수 있다. | USING은 양쪽에 같은 이름의 조인 컬럼을 한 번 지정한다. | NATURAL JOIN은 같은 이름 컬럼을 자동 사용하므로 스키마 변경에 취약할 수 있다.","NATURAL JOIN은 사용자가 ON 절에 조인 컬럼을 반드시 지정해야 한다."),
  lesson(51,"SQL 기본","셀프 조인","같은 테이블을 역할별 별칭으로 두 번 사용한다.","셀프 조인은 직원-관리자처럼 같은 엔터티 안의 계층 관계를 연결한다. | 테이블 별칭을 서로 다르게 지정해야 컬럼 역할을 구분할 수 있다. | 최상위 행의 부모 키는 NULL일 수 있으므로 외부 조인을 고려한다.","셀프 조인은 물리적으로 동일한 테이블을 두 개 복사해야만 가능하다."),
  lesson(52,"SQL 기본","EXISTS","서브쿼리 결과 행의 존재 여부만 검사한다.","EXISTS는 서브쿼리가 한 행이라도 반환하면 TRUE다. | SELECT 목록의 실제 값은 중요하지 않다. | 상관 서브쿼리와 함께 세미 조인 의미로 자주 사용한다.","EXISTS는 서브쿼리가 반환한 첫 컬럼의 숫자가 1일 때만 TRUE다.","SELECT * FROM dept d WHERE EXISTS (SELECT 1 FROM emp e WHERE e.deptno=d.deptno);"),
  lesson(53,"SQL 기본","NOT EXISTS","관련 행이 없는 대상을 안전하게 찾는다.","NOT EXISTS는 상관 조건을 만족하는 행이 하나도 없을 때 TRUE다. | NULL이 섞인 NOT IN의 UNKNOWN 문제를 피하는 대안이 된다. | 부서에 직원이 없는 경우 같은 반조인 질의에 적합하다.","NOT EXISTS는 서브쿼리 결과에 NULL이 한 번이라도 있으면 전체가 UNKNOWN이다."),
  lesson(54,"SQL 기본","IN과 NOT IN의 NULL","3값 논리가 결과를 없앨 수 있다.","IN은 여러 동등 비교의 OR와 유사하다. | NOT IN 서브쿼리에 NULL이 포함되면 비교가 UNKNOWN이 되어 기대와 달리 행이 없을 수 있다. | NULL 가능성이 있으면 제거하거나 NOT EXISTS로 의미를 분명히 한다.","NOT IN 목록에 NULL이 있어도 NULL만 무시하고 나머지 값으로 정상 비교한다."),
  lesson(55,"SQL 기본","SQL 기본 종합","논리 순서대로 결과 행 수를 추적한다.","FROM/JOIN으로 원본 집합을 만든 뒤 WHERE로 행을 거른다. | GROUP BY/HAVING으로 그룹을 만들고 거른 뒤 SELECT 결과를 계산한다. | 마지막 ORDER BY는 표시 순서만 바꾸며 행 집합 자체를 바꾸지 않는다.","ORDER BY는 WHERE보다 먼저 실행되어 필터링 대상 행을 결정한다."),
  lesson(56,"SQL 활용","단일행 서브쿼리","한 행을 반환할 때 단일행 비교 연산자를 쓴다.","단일행 서브쿼리는 =, <, > 같은 단일행 연산자와 사용한다. | 두 행 이상 반환하면 런타임 오류가 발생할 수 있다. | 집계 함수로 한 값을 만들거나 조건으로 유일성을 보장한다.","= 연산자 뒤 서브쿼리가 여러 행을 반환해도 첫 행만 자동 사용한다."),
  lesson(57,"SQL 활용","다중행 서브쿼리","IN·ANY·ALL의 의미를 계산한다.","IN은 반환 집합 중 하나와 같으면 TRUE다. | > ANY는 최솟값보다 크다는 의미, > ALL은 최댓값보다 크다는 의미로 해석할 수 있다. | 빈 집합과 NULL이 있을 때 3값 논리를 주의한다.","> ALL은 집합의 최솟값보다 크기만 하면 참이다."),
  lesson(58,"SQL 활용","다중컬럼 서브쿼리","여러 컬럼의 쌍을 동시에 비교한다.","다중컬럼 서브쿼리는 컬럼 수와 대응 타입이 맞아야 한다. | (deptno, salary) IN (...)은 두 값의 같은 행 조합을 비교한다. | 컬럼별 IN을 따로 쓰면 서로 다른 행의 값이 섞일 수 있다.","(a,b) IN 서브쿼리는 a와 b를 각각 독립된 목록으로 비교한다."),
  lesson(59,"SQL 활용","상관 서브쿼리","외부 행마다 내부 질의가 논리적으로 평가된다.","상관 서브쿼리는 내부에서 외부 쿼리 컬럼을 참조한다. | 부서별 평균보다 높은 직원 같은 그룹별 비교에 사용한다. | 논리적으로 행별 평가하지만 옵티마이저가 조인으로 변환할 수 있다.","상관 서브쿼리는 외부 쿼리의 컬럼을 참조할 수 없다."),
  lesson(60,"SQL 활용","스칼라 서브쿼리","한 행·한 컬럼 결과를 값처럼 사용한다.","스칼라 서브쿼리는 한 행 한 컬럼을 반환해야 한다. | 0행이면 NULL, 2행 이상이면 오류가 일반적이다. | SELECT 목록이나 조건식 등 단일 값이 허용되는 위치에 쓸 수 있다.","스칼라 서브쿼리가 0행이면 무조건 SQL 전체가 오류다."),
  lesson(61,"SQL 활용","인라인 뷰","FROM 절 서브쿼리로 임시 결과 집합을 만든다.","인라인 뷰는 FROM 절에 위치한 서브쿼리다. | 먼저 집계·순위·필터링한 결과를 바깥 쿼리에서 다시 다룰 때 유용하다. | 바깥에서 참조할 컬럼에는 명확한 이름을 부여한다.","인라인 뷰는 데이터베이스에 영구 저장되는 물리 테이블이다."),
  lesson(62,"SQL 활용","UNION과 UNION ALL","중복 제거 여부와 성능을 구분한다.","UNION은 합친 뒤 중복을 제거하고 UNION ALL은 중복을 유지한다. | 각 SELECT의 컬럼 수와 대응 타입이 호환되어야 한다. | 중복 제거가 필요 없으면 UNION ALL이 불필요한 정렬·해시 작업을 줄일 수 있다.","UNION ALL은 자동으로 중복 행을 제거한다."),
  lesson(63,"SQL 활용","INTERSECT와 MINUS","교집합·차집합을 계산한다.","INTERSECT는 양쪽에 공통인 행을 반환한다. | MINUS/EXCEPT는 첫 집합에는 있고 둘째에는 없는 행을 반환한다. | 집합 연산은 컬럼 위치별로 비교하며 ORDER BY는 전체 결과 마지막에 둔다.","A MINUS B와 B MINUS A는 언제나 같은 결과다."),
  lesson(64,"SQL 활용","ROLLUP","계층 순서에 따른 소계와 총계를 만든다.","ROLLUP(a,b)는 일반적으로 (a,b), (a), () 그룹을 생성한다. | 컬럼 순서가 소계 계층을 결정한다. | 상세→중간 소계→총계 보고서에 적합하다.","ROLLUP(a,b)는 (a,b), (b), () 그룹을 만든다."),
  lesson(65,"SQL 활용","CUBE","가능한 모든 컬럼 조합의 집계를 만든다.","CUBE(a,b)는 (a,b), (a), (b), () 그룹을 생성한다. | 컬럼 수가 늘면 집계 조합이 크게 증가한다. | 다차원 분석에 유용하지만 필요한 조합만 원하면 GROUPING SETS가 효율적이다.","CUBE(a,b)는 ROLLUP(a,b)와 항상 동일한 그룹만 만든다."),
  lesson(66,"SQL 활용","GROUPING SETS","필요한 집계 조합만 명시한다.","GROUPING SETS는 여러 GROUP BY 결과를 한 질의로 결합한다. | 지정하지 않은 조합은 생성하지 않는다. | 불필요한 CUBE 조합을 피하면서 보고서 소계를 만들 수 있다.","GROUPING SETS는 모든 가능한 컬럼 조합을 자동 생성한다."),
  lesson(67,"SQL 활용","GROUPING 함수","집계로 생긴 NULL과 원본 NULL을 구분한다.","GROUPING(col)은 해당 행에서 col이 집계 때문에 NULL이면 1, 아니면 0을 반환한다. | 원본 데이터의 NULL과 소계 표시용 NULL을 구별한다. | GROUPING_ID는 여러 컬럼의 집계 상태를 비트 형태 값으로 표현한다.","GROUPING(col)은 원본 값이 NULL이면 무조건 1이다."),
  lesson(68,"SQL 활용","윈도우 함수 기본","행을 유지하면서 분석값을 붙인다.","윈도우 함수는 GROUP BY와 달리 원본 행 수를 유지한다. | OVER의 PARTITION BY는 계산 그룹을 나누고 ORDER BY는 순서를 정한다. | 집계 윈도우 함수로 누계·이동 평균을 계산할 수 있다.","윈도우 함수를 사용하면 같은 파티션의 행들이 반드시 한 행으로 축약된다."),
  lesson(69,"SQL 활용","ROW_NUMBER·RANK·DENSE_RANK","동점 순위의 차이를 계산한다.","ROW_NUMBER는 동점이어도 고유한 연속 번호를 준다. | RANK는 동점 다음 순위를 건너뛴다. | DENSE_RANK는 동점 다음 순위를 건너뛰지 않는다.","RANK는 동점 뒤에도 반드시 연속된 순위를 부여한다."),
  lesson(70,"SQL 활용","LAG와 LEAD","이전·다음 행 값을 현재 행에서 참조한다.","LAG는 이전 행, LEAD는 다음 행 값을 가져온다. | PARTITION BY 경계를 넘지 않는다. | 자기 조인 없이 전월 대비·이전 거래 대비 차이를 계산할 수 있다.","LAG는 현재 행보다 다음에 오는 행의 값을 반환한다.","SELECT ym, sales, sales-LAG(sales) OVER(ORDER BY ym) diff FROM monthly_sales;"),
  lesson(71,"SQL 활용","FIRST_VALUE·LAST_VALUE","윈도우 프레임이 결과에 미치는 영향을 이해한다.","FIRST_VALUE는 프레임의 첫 값을, LAST_VALUE는 마지막 값을 반환한다. | 기본 프레임에서는 LAST_VALUE가 현재 행 값처럼 보일 수 있다. | 전체 파티션 마지막 값을 원하면 프레임 끝을 UNBOUNDED FOLLOWING으로 명시한다.","LAST_VALUE는 프레임 설정과 무관하게 항상 파티션의 최종 행 값을 반환한다."),
  lesson(72,"SQL 활용","ROWS와 RANGE","물리 행 프레임과 논리 값 프레임을 구분한다.","ROWS는 현재 행을 기준으로 물리적 행 개수를 계산한다. | RANGE는 ORDER BY 값이 같은 동료 행을 함께 포함할 수 있다. | 누계 결과에서 동점 값이 있을 때 둘의 차이가 나타난다.","ROWS와 RANGE는 동점 데이터가 있어도 언제나 동일한 결과다."),
  lesson(73,"SQL 활용","NTILE·RATIO_TO_REPORT","등분과 전체 대비 비율을 계산한다.","NTILE(n)은 정렬된 행을 가능한 균등한 n개 그룹으로 나눈다. | RATIO_TO_REPORT는 파티션 합계 대비 현재 값의 비율이다. | 행 수가 그룹 수로 나누어떨어지지 않으면 앞 그룹이 더 많은 행을 가질 수 있다.","NTILE(4)는 데이터 수와 관계없이 각 그룹 행 수가 반드시 완전히 같다."),
  lesson(74,"SQL 활용","Top-N 기본","정렬 후 제한이 적용되어야 한다.","Top-N은 먼저 원하는 기준으로 정렬하고 그 결과에서 상위 행을 제한한다. | FETCH FIRST, LIMIT 또는 인라인 뷰와 ROWNUM을 DBMS에 맞게 사용한다. | Oracle의 같은 레벨 WHERE ROWNUM과 ORDER BY 순서를 혼동하지 않는다.","Oracle에서 WHERE ROWNUM <= 10과 ORDER BY를 같은 SELECT에 쓰면 항상 전체 정렬 후 상위 10건이다."),
  lesson(75,"SQL 활용","ROWNUM","할당 시점과 불가능한 조건을 이해한다.","Oracle ROWNUM은 결과 행에 반환 과정에서 순차 부여되는 의사컬럼이다. | ROWNUM = 1은 가능하지만 ROWNUM = 2만 단독으로 시작하면 첫 행이 조건을 못 넘어 결과가 없다. | 정렬 후 번호가 필요하면 인라인 뷰를 사용한다.","WHERE ROWNUM = 2는 첫 행을 건너뛰고 두 번째 행을 정상 반환한다."),
  lesson(76,"SQL 활용","계층형 질의","START WITH·CONNECT BY·PRIOR의 역할을 구분한다.","START WITH는 루트 행을 정한다. | CONNECT BY는 부모와 자식의 연결 규칙을 정하고 PRIOR 위치가 탐색 방향을 좌우한다. | LEVEL은 루트 1부터 계층 깊이를 나타낸다.","LEVEL은 루트 행에서 0으로 시작한다."),
  lesson(77,"SQL 활용","계층 경로와 리프","CONNECT_BY 계열 의사컬럼·함수를 익힌다.","CONNECT_BY_ISLEAF는 자식이 없는 리프 행을 표시한다. | SYS_CONNECT_BY_PATH는 루트부터 현재 행까지 경로를 만든다. | NOCYCLE은 순환 데이터로 인한 무한 탐색 오류를 방지한다.","CONNECT_BY_ISLEAF는 루트 노드이면 항상 1이다."),
  lesson(78,"SQL 활용","PIVOT","행의 값을 열로 회전하며 집계한다.","PIVOT은 지정 컬럼 값별로 집계 결과를 열로 만든다. | 집계 함수와 회전할 값 목록을 지정한다. | 보고서 가독성은 높지만 고정 열 목록 때문에 값 변화 대응을 고려한다.","PIVOT은 집계 없이 모든 원본 행을 그대로 보존하며 방향만 바꾼다."),
  lesson(79,"SQL 활용","UNPIVOT","여러 열을 행 형태로 변환한다.","UNPIVOT은 컬럼들을 이름·값 행으로 펼친다. | 기본적으로 NULL 값 제외 여부를 문법으로 제어할 수 있다. | 넓은 보고서형 데이터를 정규화된 분석 형태로 바꿀 때 유용하다.","UNPIVOT은 여러 행을 새 컬럼으로 만드는 연산이다."),
  lesson(80,"SQL 활용","정규 표현식 기초","문자 클래스·수량자·앵커를 읽는다.","^와 $는 문자열 시작과 끝을 뜻한다. | []는 문자 클래스, *는 0회 이상, +는 1회 이상 반복이다. | REGEXP_LIKE·REPLACE·SUBSTR 등으로 검증·치환·추출한다.","정규식 ^A는 문자열 어디든 A가 한 번 포함되면 일치한다."),
  lesson(81,"SQL 활용","JOIN과 서브쿼리 변환","같은 요구를 집합 관점에서 다시 쓴다.","존재 확인은 EXISTS, 관련 값 결합은 JOIN이 자연스러운 경우가 많다. | JOIN으로 1:M을 연결하면 기준 행이 중복될 수 있다. | DISTINCT로 증상을 숨기기 전에 관계와 요구 행 단위를 확인한다.","1:M JOIN에서도 1쪽 행은 언제나 정확히 한 번만 나타난다."),
  lesson(82,"SQL 활용","MERGE","조건에 따라 갱신과 삽입을 한 문장으로 처리한다.","MERGE는 원본과 대상의 매칭 조건에 따라 UPDATE와 INSERT를 분기한다. | 원본에서 대상 한 행과 여러 번 매칭되면 DBMS별 오류 위험이 있다. | 동기화·업서트에 유용하지만 조인 조건의 유일성을 확인한다.","MERGE는 조회 전용 문장이므로 대상 테이블을 변경하지 않는다."),
  lesson(83,"SQL 활용","윈도우 함수 종합","파티션·정렬·프레임을 순서대로 해석한다.","PARTITION BY가 없으면 전체 결과가 하나의 파티션이다. | ORDER BY가 순위와 누계 순서를 결정한다. | 프레임 절은 현재 행별 계산에 실제 포함되는 행 범위를 정한다.","윈도우 함수의 ORDER BY는 최종 결과 출력 순서까지 항상 보장한다."),
  lesson(84,"SQL 활용","집합과 NULL 종합","중복·NULL·행 수를 손으로 계산한다.","UNION 계열의 중복 판단에서는 NULL 행도 중복 제거 대상이 될 수 있다. | 집계 함수별 NULL 제외 여부를 구별한다. | 조건식의 UNKNOWN은 WHERE를 통과하지 못한다.","WHERE 조건 결과가 UNKNOWN인 행은 TRUE와 같이 결과에 포함된다."),
  lesson(85,"SQL 활용","SQL 활용 종합","복잡한 질의를 블록별 행 집합으로 분해한다.","인라인 뷰·서브쿼리마다 컬럼과 예상 행 수를 먼저 적는다. | 조인에서 1:1, 1:M, M:N 증폭을 계산한다. | 마지막에 NULL과 중복 제거, 정렬, Top-N을 적용해 답을 확정한다.","복잡한 SQL은 SELECT 절부터 문자 순서대로 실행 결과를 계산해야 한다."),
  lesson(86,"관리·실전","INSERT","컬럼 목록과 제약조건을 확인한다.","INSERT 컬럼 목록을 생략하면 테이블 정의 순서 전체에 맞춰 값을 제공해야 한다. | NOT NULL·PK·FK·CHECK 제약을 만족해야 한다. | INSERT SELECT로 조회 결과 여러 행을 입력할 수 있다.","INSERT는 한 문장에 반드시 한 행만 입력할 수 있다."),
  lesson(87,"관리·실전","UPDATE와 DELETE","WHERE 누락의 영향을 구별한다.","UPDATE는 지정 컬럼 값을 변경하고 DELETE는 행을 제거한다. | WHERE가 없으면 대상 테이블의 모든 행이 영향을 받는다. | 둘 다 일반적으로 트랜잭션 제어 대상 DML이다.","DELETE에 WHERE가 없으면 테이블 구조까지 함께 삭제된다."),
  lesson(88,"관리·실전","DDL","CREATE·ALTER·DROP·TRUNCATE를 구분한다.","CREATE는 객체 생성, ALTER는 구조 변경, DROP은 객체 제거다. | TRUNCATE는 모든 행을 빠르게 제거하고 구조는 남긴다. | Oracle에서는 DDL 전후 암시적 COMMIT 등 트랜잭션 특성을 주의한다.","TRUNCATE TABLE은 테이블 정의와 인덱스까지 객체 자체를 제거한다."),
  lesson(89,"관리·실전","DELETE·TRUNCATE·DROP","데이터·구조·롤백 가능성을 비교한다.","DELETE는 조건 지정이 가능한 DML이다. | TRUNCATE는 전체 행을 제거하는 DDL이며 일반적으로 롤백이 어렵다. | DROP은 테이블 객체 자체를 제거한다.","DELETE, TRUNCATE, DROP은 조건절 사용과 롤백 특성이 완전히 동일하다."),
  lesson(90,"관리·실전","제약조건","PK·UNIQUE·FK·CHECK·NOT NULL을 구분한다.","PRIMARY KEY는 유일성과 NOT NULL을 함께 보장한다. | UNIQUE는 중복을 제한하지만 NULL 허용 방식은 DBMS별로 살펴본다. | FOREIGN KEY는 부모 후보키 값 참조 또는 NULL을 통해 참조 무결성을 지킨다.","FOREIGN KEY 컬럼은 어떤 경우에도 NULL을 가질 수 없다."),
  lesson(91,"관리·실전","뷰","저장된 질의로 논리적 데이터 창을 제공한다.","뷰는 질의 정의를 저장해 보안·편의·논리적 독립성을 제공한다. | 일반 뷰는 보통 자체 데이터를 별도로 저장하지 않는다. | 집계·조인·DISTINCT가 포함된 복잡한 뷰는 갱신이 제한될 수 있다.","모든 뷰는 원본과 별도로 실제 행 데이터를 반드시 저장한다."),
  lesson(92,"관리·실전","DCL","GRANT와 REVOKE로 권한을 관리한다.","GRANT는 객체 또는 시스템 권한을 부여한다. | REVOKE는 부여된 권한을 회수한다. | 역할은 여러 권한을 묶어 사용자에게 효율적으로 부여한다.","REVOKE는 사용자에게 새로운 권한을 추가하는 명령이다."),
  lesson(93,"관리·실전","트랜잭션 ACID","원자성·일관성·고립성·지속성을 구분한다.","원자성은 트랜잭션 작업이 전부 성공하거나 전부 취소됨을 뜻한다. | 일관성은 제약과 규칙을 만족하는 상태 전이를 뜻한다. | 고립성은 동시 트랜잭션 간 간섭 통제, 지속성은 커밋 결과 보존이다.","지속성은 여러 트랜잭션이 서로의 중간 결과를 자유롭게 볼 수 있다는 뜻이다."),
  lesson(94,"관리·실전","COMMIT과 ROLLBACK","변경 확정과 취소 범위를 판단한다.","COMMIT은 현재 트랜잭션 변경을 영구 확정한다. | ROLLBACK은 커밋되지 않은 변경을 취소한다. | COMMIT 뒤에는 일반적인 ROLLBACK으로 이전 상태를 되돌릴 수 없다.","COMMIT한 변경은 같은 세션에서 ROLLBACK하면 항상 취소된다."),
  lesson(95,"관리·실전","SAVEPOINT","트랜잭션 일부만 되돌린다.","SAVEPOINT는 트랜잭션 내부의 중간 지점을 지정한다. | ROLLBACK TO SAVEPOINT는 해당 지점 이후 변경을 취소한다. | 전체 트랜잭션은 계속 유지되며 이후 COMMIT할 수 있다.","SAVEPOINT를 만들면 그 시점까지 변경이 자동 COMMIT된다."),
  lesson(96,"관리·실전","시험 시간 전략","50문항을 90분 안에 안정적으로 푼다.","1차 풀이에서 정의·단문항을 빠르게 확보하고 계산형에 표시한다. | 과목1은 10문항 중 최소 4문항을 맞혀야 과락을 피한다. | 마지막에는 NULL·조인 행 수·논리 실행 순서를 우선 재검산한다.","총점만 60점이면 한 과목이 0점이어도 합격한다."),
  lesson(97,"관리·실전","행 수 계산법","조인 전후 카디널리티를 표로 추적한다.","각 쿼리 블록의 시작 행 수를 적고 필터 선택도를 적용한다. | PK:FK 1:M 조인은 보통 M쪽 매칭 행 수를 중심으로 센다. | 외부 조인은 보존측 미매칭 행을 추가하고 WHERE에서 다시 제거되는지 본다.","외부 조인의 행 수는 항상 두 테이블 행 수를 단순히 더한 값이다."),
  lesson(98,"관리·실전","NULL 함정 점검","비교·연산·집계·정렬의 NULL을 따로 확인한다.","NULL 비교는 IS NULL, 조건 결과는 3값 논리로 판단한다. | 산술 NULL 전파와 집계 NULL 제외를 구분한다. | NOT IN, 외부 조인 WHERE 조건, COUNT 컬럼이 빈출 함정이다.","NULL은 빈 문자열·0과 동일하므로 별도 점검할 필요가 없다."),
  lesson(99,"관리·실전","SQL 손풀이 절차","문법이 아니라 중간 결과 테이블을 그린다.","FROM과 JOIN 결과를 먼저 그리고 WHERE 통과 행만 남긴다. | 그룹별 집계와 HAVING을 계산한 뒤 SELECT 표현식을 만든다. | 집합·윈도우·정렬·Top-N 단계가 있으면 마지막에 각각 적용한다.","SQL은 작성 순서대로 SELECT부터 계산하면 가장 정확하다."),
  lesson(100,"관리·실전","최종 합격 체크","공식 범위 전체를 약점 중심으로 회독한다.","모델링에서는 엔터티·속성·관계·식별자·정규화를 설명할 수 있어야 한다. | SQL에서는 NULL·조인·서브쿼리·집계·윈도우·관리 구문 결과를 손으로 계산해야 한다. | 오답은 개념 부족, 실행 순서, NULL, 행 수, DBMS 차이 중 원인을 태깅해 재학습한다.","문제 수가 적은 데이터 모델링 과목은 과락 위험이 없으므로 버려도 된다.")
];
